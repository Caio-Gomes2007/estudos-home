// Escreva um programa em C para calcular a soma de dois números.
#include <stdio.h>

int soma(int x, int y)
{
    int result = x + y;
    return result;
}
int main()
{
    printf("%i", soma(5, 5));
    return 0;
}